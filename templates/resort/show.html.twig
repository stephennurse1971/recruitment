{% extends 'base.html.twig' %}

{% block title %}Resort{% endblock %}

{% block body %}
    <h1>Resort</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resort.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ resort.description }}</td>
            </tr>
            <tr>
                <th>Maplink</th>
                <td>{{ resort.maplink }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resort_index') }}">back to list</a>

    <a href="{{ path('resort_edit', {'id': resort.id}) }}">edit</a>

    {{ include('resort/_delete_form.html.twig') }}
{% endblock %}
