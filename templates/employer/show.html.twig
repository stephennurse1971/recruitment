{% extends 'base.html.twig' %}

{% block title %}Employer{% endblock %}

{% block body %}
    <h1>Employer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employer.id }}</td>
            </tr>
            <tr>
                <th>EmployerName</th>
                <td>{{ employer.employerName }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ employer.address }}</td>
            </tr>
            <tr>
                <th>Email1</th>
                <td>{{ employer.email1 }}</td>
            </tr>
            <tr>
                <th>Email2</th>
                <td>{{ employer.email2 }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ employer.telephone }}</td>
            </tr>
            <tr>
                <th>AboutUs</th>
                <td>{{ employer.aboutUs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('employer_index') }}">back to list</a>

    <a href="{{ path('employer_edit', {'id': employer.id}) }}">edit</a>

    {{ include('employer/_delete_form.html.twig') }}
{% endblock %}
