{% extends 'admin/base.html.twig' %}

{% block title %}Edit Job{% endblock %}

{% block content %}
    <h1>Edit Job</h1>

    {{ include('job/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn-outline-info" href="{{ path('job_index') }}">Return to list</a>

    {{ include('job/_delete_form.html.twig') }}
{% endblock %}
